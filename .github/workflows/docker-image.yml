name: Docker Image CI

on:
  repository_dispatch:
    types: [ master-updated ]
  workflow_dispatch:
  push:
    branches:
      - 'master'


env:
  REGISTRY: ghcr.io

jobs:
  ledgersmb:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Log in to the Container registry
      uses: docker/login-action@c144859092e8e353f3e118b0b329679a62351c3e
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@eb6512707bea5740b2608f2a43ece7bc543cb75b
      with:
        context: ledgersmb/
        push: true
        tags: ${{ env.REGISTRY }}/ledgersmb-devel/dev-ledgersmb:latest

  proxy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Log in to the Container registry
      uses: docker/login-action@c144859092e8e353f3e118b0b329679a62351c3e
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@eb6512707bea5740b2608f2a43ece7bc543cb75b
      with:
        context: nginx/
        push: true
        tags: ${{ env.REGISTRY }}/ledgersmb-devel/dev-nginx:latest

  postgres:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        pg_version: [ "latest", "18", "17", "16", "15", "14", "13", "12" ]
    permissions:
      contents: read
      packages: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Log in to the Container registry
      uses: docker/login-action@c144859092e8e353f3e118b0b329679a62351c3e
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@eb6512707bea5740b2608f2a43ece7bc543cb75b
      with:
        build-args: |
          PGBASE=${{ matrix.pg_version }}
        context: postgres/
        push: true
        tags: ${{ env.REGISTRY }}/ledgersmb-devel/dev-postgres:${{ matrix.pg_version }}

  cleanup:
    runs-on: ubuntu-latest
    needs: [ 'ledgersmb', 'proxy', 'postgres' ]
    permissions:
      contents: read
      packages: write
    steps:
    - name: ghcr.io cleanup action
      uses: dataaxiom/ghcr-cleanup-action@v1
      with:
        packages: dev-postgres,dev-ledgersmb,dev-nginx
